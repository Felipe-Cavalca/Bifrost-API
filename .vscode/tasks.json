{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Mesclar .github/latest-release com a branch atual",
            "type": "shell",
            "problemMatcher": [],
            "windows": {
                "command": "if (git remote | findstr 'github') { git remote set-url github https://github.com/Felipe-Cavalca/.github.git } else { git remote add github https://github.com/Felipe-Cavalca/.github.git }; git fetch github; git merge github/latest-release --allow-unrelated-histories"
            },
            "linux": {
                "command": "git remote | grep github && git remote set-url github https://github.com/Felipe-Cavalca/.github.git || git remote add github https://github.com/Felipe-Cavalca/.github.git; git fetch github && git merge github/latest-release --allow-unrelated-histories"
            }
        },
        {
            "label": "Validar monorepo Composer",
            "type": "shell",
            "command": "sh .devcontainer/check.sh",
            "problemMatcher": [],
            "group": {
                "kind": "test",
                "isDefault": true
            }
        },
        {
            "label": "Subir servicos auxiliares",
            "type": "shell",
            "command": "docker compose -f .devcontainer/docker-compose.services.yml up -d",
            "problemMatcher": [],
            "runOptions": {
                "reevaluateOnRerun": true
            }
        },
        {
            "label": "Encerrar servicos auxiliares",
            "type": "shell",
            "command": "docker compose -f .devcontainer/docker-compose.services.yml down --volumes",
            "problemMatcher": [],
            "runOptions": {
                "reevaluateOnRerun": true
            }
        }
    ]
}
