# bifrost/database-mysql

Extensao opcional MySQL baseada em `bifrost/database-pdo`.

Registre `MySqlExtension` com `host`, `port`, `database`, `charset`,
`username`, `password` e `options`. A chave `connections` habilita conexoes
nomeadas.
